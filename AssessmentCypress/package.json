{
  "dependencies": {
    "cypress": "9.7.0",
    "cypress-xpath": "^2.0.1"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.8.2"
  },
  "scripts": {
    "test": "npm install && npx cypress run --config-file cypress.json",
    "test:headed": "npm install && npx cypress run --config-file cypress.json --browser chrome "
  }
}
